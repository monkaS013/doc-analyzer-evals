# Copy to .env and fill in. .env is gitignored.
ANTHROPIC_API_KEY=

# Optional: override the model (default: claude-haiku-4-5-20251001)
DOCANALYZER_MODEL=claude-haiku-4-5-20251001

# Optional: F1 threshold the eval gate requires (default 0.8)
EVAL_MIN_F1=0.8
